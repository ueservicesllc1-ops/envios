import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'comm.freedomlabs.enviosapp',
  appName: 'Envios Ecuador',
  webDir: 'build'
};

export default config;
